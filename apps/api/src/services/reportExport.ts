// Phase 2 placeholder — full PDF generation via @react-pdf/renderer or puppeteer
export function generatePdfBuffer(_data: unknown): Buffer {
  return Buffer.from('%PDF-1.4 stub — implement in Phase 2')
}
